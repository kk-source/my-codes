# include<stdio.h>
    int main(){

    int year;
    printf("enter year\n");
    scanf("%d", &year);

    if( (year)%4==0 ) {
        printf("entered year is a leap year");
    
    }
    else {
        printf("entered year is a non-leap year");
    }

    return 0;
} // program is wrong check google for more details.