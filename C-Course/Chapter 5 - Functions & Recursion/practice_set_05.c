# include<stdio.h>
    int main(){

    int a=3;
    printf("%d %d %d",a,++a,a++);
    /* as argument (value of a) is passed from right to 
    left upon execution while control is with printf */
    return 0;
}